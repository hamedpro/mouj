import { Component } from "react";

export default class SupportMessages extends Component{
    render(){
        return(
            <div className="text-white">here is support messages root</div>
        )
    }
}